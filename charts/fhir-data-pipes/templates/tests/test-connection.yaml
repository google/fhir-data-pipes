apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fhir-data-pipes.fullname" . }}-test-connection"
  labels:
    {{- include "fhir-data-pipes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fhir-data-pipes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
